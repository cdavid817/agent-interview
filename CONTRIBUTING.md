# 贡献指南

本项目优先接受可验证、可复现、能帮助候选人建立工程判断的内容。

## 选择位置

1. 在 `docs/01-foundations`～`docs/04-products` 中选择唯一主章节。
2. 将题目放入最匹配的子主题文件；单文件不得超过 50 题。
3. 跨领域问题不要复制，在其他章节建立稳定 ID 链接。
4. 提交前检查[重复题候选报告](docs/reference/duplicate-questions.md)。

## 题目格式

题目遵循[题目结构规范](docs/00-guide/question-schema.md)，使用全局唯一稳定 ID：

```markdown
<a id="plan-308"></a>
### PLAN-308 · 问题内容？

> 稳定 ID：`PLAN-308`

参考答案……

**相关知识点：** 术语一、术语二。
```

稳定 ID 一经发布不得修改或复用。新增题使用对应章节当前最大序号加一；删除题后保留序号空洞，不进行整体重排。

## 答案要求

- 第一段直接回答问题；
- 展开核心机制或执行流程；
- 说明适用边界、失败模式和工程权衡；
- 给出可以验证方案的指标；
- 每题保留且仅保留一处“相关知识点”；
- 协议、产品和框架能力优先引用官方资料，并记录版本或核验日期；
- 不把 HTTP 成功、模型声明完成或单一 LLM Judge 分数当作业务正确性。

## 提交流程

1. 修改对应子主题文件；
2. 更新必要的章节索引和交叉链接；
3. 涉及术语时运行 `pwsh -File scripts/build_glossary.ps1`；
4. 运行 `python scripts/build_indexes.py`；
5. 运行 `python scripts/validate.py`；
6. 在 Pull Request 中说明稳定 ID、修改原因和验证依据。

大范围删除或合并重复题前请先讨论，并在迁移说明中记录旧 ID 到主问题 ID 的映射。
